# ── Secrets & real data (NEVER commit) ──────────────────────────
# users.db holds real user emails + password hashes
.env
*.db
# user-uploaded cert images
uploads/

# ── Runtime / generated ─────────────────────────────────────────
# MyeHalal query cache (regenerated by central_check.py)
myehalal_cache.json
*.log
# data.json assembled at runtime / on the VPS
data.json

# ── Python ──────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/
.testvenv/
senangwei_local/.testvenv/

# ── OS / editor ─────────────────────────────────────────────────
.DS_Store

# ── Scraping scratch / intermediate artifacts ───────────────────
scratchpad/
pages/
scratch_*
scratchpad_*
s.tmp
*.html
prod1.json
bf1.json
buildingData.json
buildingConfig.json
tenant_details.json
tenants.json
handles.txt
abuzzWebAPI.js
customabuzz.js
boost_init.js
sm.xml
smc.xml
